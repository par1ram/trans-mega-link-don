import { useState } from "react"
import './Button.css'

const Button = ({  }) => {
    const [mail, setMail] = useState('mail')

    return (
        <>
            <button onClick={() => {}}>
                mail
            </button>
        </>
    )
}

export default Button
